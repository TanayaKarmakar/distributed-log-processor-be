# docker/docker-compose.yml
version: '3'

services:
  log-storage:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - ../logs:/app/logs
    ports:
      - "9000:9000"  # For future web interface